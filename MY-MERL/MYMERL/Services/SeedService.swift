import Foundation
import SwiftData

enum SeedService {
    @MainActor static func seedIfNeeded(_ context: ModelContext) {
        // La beta definitiva parte intenzionalmente vuota: nessun dato dimostrativo.
    }
}
